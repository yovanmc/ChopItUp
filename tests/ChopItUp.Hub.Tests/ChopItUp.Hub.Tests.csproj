<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.SignalR.Client" Version="10.0.11" />
    <PackageReference Include="ModelContextProtocol" Version="2.2.0" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\src\ChopItUp.Hub\ChopItUp.Hub.csproj" />
    <!-- DryRunTests.cs uses the same corpus builder tools/Invoke-M2DryRun.ps1 invokes, so the
         script and dotnet test cannot drift (M2 Task 6, pass 2 MAJOR-9 part 2). -->
    <ProjectReference Include="..\..\tools\ChopItUp.Corpus\ChopItUp.Corpus.csproj" />
  </ItemGroup>
</Project>
