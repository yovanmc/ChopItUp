# Bugs, flakes and chores (worked by the fix-bug skill; never a board row)

- 60 LEAD: the unnamed Hub failure from 2026-09-19 (915/916, test name not retained) remains unidentified. The later named ExchangeApiTests stop/snapshot races, including former items 62 and 63, were corrected and verified in PR #137 (`cd1cf4b`, CI run 35654079714); do not rebuild those fixes. Whether the first failure had the same cause is unknown.
- 61 chore: a client showed `read_messages`/`wait_for_message` `room_id` and `limit` and `propose_memory` `replaces` as required; the hub's own tools/list marks all three optional with defaults (measured 2026-09-19 at 6ab2679 via HubTestHost.ClientFor + ListToolsAsync). LEAD: which client rendered them required, and whether it reads `default` at all.
- 66 FIXED: selector over-selected: `docs/affected-tests.md` hits the `affected-tests[^/]*` contract guard (`tools/affected-tests.mjs:67`) and selects full; any `src/ChopItUp.Hub/client/` edit resolves to the Hub project by directory prefix (`:84`) and selects Hub.Tests + Desktop.Tests besides the client · guard narrowed to affected-tests.(mjs|json|test.mjs); clientSubtrees rule before the project-directory match selects client + registered contract readers (none; 6 reviewed non-readers) + a Hub build for ClientBuild typecheck/bundle · CSS-only gate, 3 runs each, -Base HEAD: before median 912.2 s (881.6-1096.9, Hub+Desktop+client; run 3 failed on an unrelated socket error), after median 5.9 s (5.6-12.2; run 1 = fresh client build) · [V 2026-09-22 a7911bf] merged d6cd7a9 (#141)
- 67 LEAD: `MemoryEditApiTests.AC2_the_editor_can_shrink_a_topic_already_over_its_cap` failed 12 min into a 15 min Hub.Tests run on main a7911bf with SocketException "insufficient buffer space or queue was full" (127.0.0.1:65533) · likely loopback ephemeral-port exhaustion late in the run; the same suite passed twice minutes earlier; rate unmeasured · 2026-09-22, 22 further Hub runs on the desk: the same error appeared at serial, at 2 and at 4 parallel collections, so it tracks the machine's socket budget rather than the test concurrency. Twice it hit `HubHost` startup, and because hosting throws it on its own thread the whole test host died and the run aborted (Application event log, `.NET Runtime`, "Hosting failed to start ... SocketException (10055)"). Both aborts happened while unrelated network-heavy apps were running. TIME_WAIT stayed between 74 and 190, so the exhausted budget is not ephemeral ports alone
- 71 LEAD: `DeployScriptTests.Deploy_aborts_and_changes_nothing_when_a_process_is_running_from_the_target(forwardSlashes: False)` failed on the CI runner with `Assert.Equal` "Expected C:\Users\runneradmin\...\guard exe, Actual C:\Windows\SYSTEM32\ntdll.dll" (`DeployScriptTests.cs:180`, same shape at `:673`). `WaitForMainModulePath` (`:177-179`) already polls because Windows fills the module list asynchronously, but it accepts the first non-null value, and early in process start that value can be `ntdll.dll` rather than the image. Fix: keep polling until the path is not a system DLL, or until it matches, and fail on the timeout instead. Seen once in 6 CI Hub runs, always under load
- 68 LEAD: `SpawnerServiceTests.R35_two_exchanges_edit_at_once_and_both_merge_into_the_room_on_conclusion` failed in CI run 35728266405 (PR 141, Hub code identical to main a7911bf): TimeoutException "No matching message in lab within 00:00:15" · 983/984 passed · likely timing-sensitive under the CI runner; rate unmeasured · 2026-09-22: the fixed 15 s waits in the SpawnerService classes are the first thing to fail whenever the desk is loaded. With a looping build on the other cores, 5 of 1000 failed (timeouts plus two "file in use" cleanups); with a second test suite running, 6 of 1000, all in `SpawnerServiceRunsTests`. The same runs pass on an idle desk, so a wait that scales with observed load, rather than a fixed 15 s, is the fix to consider
- 72 FIXED: `SkillImportTests.Validate_refuses_with_IoFailure_when_the_store_mutex_is_held_by_another_import` waited out the production mutex timeout on every ordinary run; the wait was `private static readonly TimeSpan MutexTimeout` with no injection point · `Validate` now takes an optional `mutexTimeout` defaulting to the readable `SkillImport.DefaultMutexTimeout`, which no shipped caller passes · that test injects 1 s against the same real named mutex on the same separate OS thread (10.01 s before, 1 s after), a contract test pins the 10 s default without waiting, and a sentinel keeps the real-duration assertion under `Category=RealDuration`, which `Invoke-AffectedTests.ps1` excludes unless `-IncludeRealDuration` · [V 2026-09-22 464ed75a] merged 43bb653 (#146)
- 73 `Invoke-Row28SelfCheck.ps1` leg `health.responds-200-expected-schema` fails against any current build: it expects schema 13 while `ChopDb.LatestSchemaVersion` is 15 · `tools/Invoke-Row28SelfCheck.ps1:227` "`$expectedSchema = 13`", `src/ChopItUp.Core/Storage/ChopDb.cs:10` "`LatestSchemaVersion = 15`" · [V 2026-09-23 2eb30477]
- 74 `Invoke-M23MemoryCheck.ps1` row `health.schema` fails by default against a v15 hub · `tools/Invoke-M23MemoryCheck.ps1:27` "`[int]$ExpectedSchema = 13`" · [V 2026-09-23 2eb30477]
- 75 `Invoke-Row14RolesCheck.ps1` contradicts itself: it asserts the hub reports schema 15, then that the same migrated database is stamped 14 · `tools/Invoke-Row14RolesCheck.ps1:304` "`health.schema-is-15`", `:308` "`migrated.stamped-v14`" · [V 2026-09-23 2eb30477]
- 76 `Invoke-M25DryRun.ps1` expects the migrated database stamped 14 while `ChopDb.LatestSchemaVersion` is 15 · `tools/Invoke-M25DryRun.ps1:464` "`migrated.stamped-v14`" · [V 2026-09-23 44b0e6b3]
- 77 `RunToolsTests.run_gate_reports_progress_every_interval_while_the_script_runs` flakes on a loaded CI runner: a 200 ms fake gate against a 40 ms progress interval must yield at least 3 reports, and CI saw 2 on a comment-only change that passes locally · `tests/ChopItUp.Hub.Tests/Mcp/RunToolsTests.cs:442` "`reports.Count >= 3`" · [V 2026-09-23 703aa3d0]
