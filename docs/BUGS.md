# Bugs, flakes and chores (worked by the fix-bug skill; never a board row)

- 60 flake: one Hub test failed once in a full-solution run (915/916, 2026-09-19, name not captured by the filtered output); the same binaries passed 916/916 on re-run. Second occurrence `[V 2026-09-19 cff58f2]` on CI for a ROADMAP.md-only diff (run 35445533659, Hub 924/925): `ExchangeApiTests.A6_A7_stop_kills_the_running_spawn_notes_it_and_every_change_reaches_signalr` at tests/ChopItUp.Hub.Tests/ExchangeApiTests.cs:75, `Assert.Equal` expected `["fable"]`, actual empty. Same named failure recurred on docs-only closeout 16067b4, CI run 35468262066 attempt 1. LEAD: whether the first occurrence was the same test.
- 61 chore: a client showed `read_messages`/`wait_for_message` `room_id` and `limit` and `propose_memory` `replaces` as required; the hub's own tools/list marks all three optional with defaults (measured 2026-09-19 at 6ab2679 via HubTestHost.ClientFor + ListToolsAsync). LEAD: which client rendered them required, and whether it reads `default` at all.
- 62 flake lead: `[V 2026-09-19 073c8a6]` full Hub run failed `SpawnerServiceTests.R44_continue_requeues_the_refused_hand_off_and_prompts_it_as_a_continuation` at tests/ChopItUp.Hub.Tests/Spawning/SpawnerServiceTests.cs:267: expected snapshot `(open, 9)`, actual `(concluded, 5)`, after observing the continuation note. LEAD: the note may become readable before the loop publishes the updated snapshot; neither that test nor the continuation branch changed in milestone 48. Isolated rerun passed with the rebuilt prompt tests (44/44); keep queued until the synchronization cause is resolved.
- 63 flake lead: `[V 2026-09-19 16067b4]` docs-only closeout CI run 35468262066 attempt 2 failed `ExchangeApiTests.A7_row27_a_run_driven_stop_marks_stoppedBy_run_on_the_wire` at tests/ChopItUp.Hub.Tests/ExchangeApiTests.cs:143: expected stoppedBy `run`, actual null, after observing a parked note. Feature CI passed the same code. LEAD: note visibility precedes the refreshed exchange snapshot; the test and run-stop path were not changed by milestone 48. Isolated rerun passed 1/1 (run-stop-flake.trx).
- 64 BLOCKED: release pending · `[V 2026-09-20 2df96c4]` PR #133 CI run 35497666109 failed `MemoryExportWriterTests.T3_UniqueTimestampedPrevious_disambiguates_a_forced_collision_on_the_name_it_just_returned`: expected a previous second's `-3`, got the next second's plain name. A fixed-UTC-instant regression now checks `-2`/`-3` and rollover separately; final CI, merge and deployment remain before queue closeout.
- 65 BLOCKED: release pending · `[V 2026-09-20 95b7138]` PR #134 CI run 35502895641 attempt 1 failed `SpawnerServiceTests.R35_a_superseded_exchange_merges_when_its_last_spawn_ends` at `SpawnerServiceTests.Rooms.cs:386`: expected `superseded`, actual `open` after the unknown-skill note. `SpawnerService.OnMessage` posts that note before publishing the updated snapshot; the test now waits for the specific exchange's published state before checking its merge. Final CI, merge and deployment remain before queue closeout.
